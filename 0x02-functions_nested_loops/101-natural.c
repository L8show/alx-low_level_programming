#include"main.h"

/**
 * main - adasdadsadasdasdd 10 adads
 *
 * Description: adasdasdasd
 *
 * Return: ae'lae 1 ad['aods 0 dads
*/

int main(void)
{
	int sum, num;

	for (num = 0; num < 1024; num++)
	{
		if ((num % 3 == 0) || (num % 5 == 0))
			sum += num;
	}
	printf("%d\n", sum);

	return (0);
}
